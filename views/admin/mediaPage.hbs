<!DOCTYPE html>
<html>
<head>
  <title>Small Image Gallery</title>
  <!-- Include Bootstrap CSS -->
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/twitter-bootstrap/5.3.0/css/bootstrap.min.css">
</head>
<body>
    {{>adminheader}}
  <div class="container">
    <h4 class="mt-5">Media Library</h4>
    <div class="row mt-4">
      <!-- Image Cards -->
      {{#each mediaData}}
      <div class="col-md-2 mb-4">
        <div class="card">
          <img src="{{media_path}}" class="card-img-top" alt="Image">
                                <input class="form-control" type="text" value="{{media_path}}" readonly>

          </div>

        </div>
      </div>
      {{/each}}
    </div>

    <!-- Pagination -->
    {{!-- <nav aria-label="Image Gallery Pagination">
      <ul class="pagination justify-content-center">
        <li class="page-item {{#if (eq currentPage 1)}}disabled{{/if}}">
          <a class="page-link" href="?page={{currentPage - 1}}" tabindex="-1" aria-disabled="{{#if (eq currentPage 1)}}true{{/if}}">Previous</a>
        </li>
        {{#each totalPages as |page|}}
        <li class="page-item {{#if (eq page currentPage)}}active{{/if}}">
          <a class="page-link" href="?page={{page}}">{{page}}</a>
        </li>
        {{/each}}
        <li class="page-item {{#if (eq currentPage totalPages)}}disabled{{/if}}">
          <a class="page-link" href="?page={{currentPage}}">Next</a>
        </li>
      </ul>
    </nav> --}}
  </div>

  <!-- Include Bootstrap JS -->
  <script src="https://cdnjs.cloudflare.com/ajax/libs/twitter-bootstrap/5.3.0/js/bootstrap.min.js"></script>
</body>
</html>
